export const WORKBLAD_V2_SCRIPT = String.raw`<script>(function(){
var DB_NAME='talera-workblad-v2',STORE='drafts',draftTimer=null,draftLoaded=false,redirectTimer=0,dateOverlay=null,dateMode='exact';
var TIMELINE_URL='https://talera-timeline-prototype.mark-a39.workers.dev/';
function ensure(){
  if(typeof state.workTitle!=='string')state.workTitle='';
  if(typeof state.workDate!=='string')state.workDate='';
  if(typeof state.workText!=='string')state.workText='';
  if(typeof state.workError!=='string')state.workError='';
  if(!Array.isArray(state.workMedia))state.workMedia=[];
  if(!Array.isArray(state.newPhotoFiles))state.newPhotoFiles=[];
  if(typeof state.editingStoryId!=='string')state.editingStoryId='';
  if(typeof state.editingToken!=='string')state.editingToken='';
  if(typeof state.hasExistingAudio!=='boolean')state.hasExistingAudio=false;
  if(typeof state.voiceAttempted!=='boolean')state.voiceAttempted=false;
  if(typeof state.voiceStarting!=='boolean')state.voiceStarting=false;
  state.sourceMode='workblad';
}
function fmt(v){v=Math.max(0,Math.round(Number(v)||0));return Math.floor(v/60)+':'+String(v%60).padStart(2,'0')}
function escAttr(s){return esc(String(s||''))}
function sleep(ms){return new Promise(function(resolve){setTimeout(resolve,ms)})}
function dbOpen(){return new Promise(function(ok,no){if(!indexedDB)return no(new Error('no db'));var r=indexedDB.open(DB_NAME,1);r.onupgradeneeded=function(){if(!r.result.objectStoreNames.contains(STORE))r.result.createObjectStore(STORE,{keyPath:'id'})};r.onsuccess=function(){ok(r.result)};r.onerror=function(){no(r.error)}})}
function revokeMediaUrls(){ensure();state.workMedia.forEach(function(m){if(m&&m.localUrl)try{URL.revokeObjectURL(m.localUrl)}catch(e){}})}
function mediaFromFiles(files){return files.map(function(file,index){return {kind:'local',file:file,localUrl:URL.createObjectURL(file),name:file.name||('foto-'+(index+1))}})}
async function draftLoad(){
  ensure();
  try{var db=await dbOpen();var d=await new Promise(function(ok,no){var r=db.transaction(STORE,'readonly').objectStore(STORE).get('current');r.onsuccess=function(){ok(r.result||null)};r.onerror=function(){no(r.error)}});db.close();if(d){state.workTitle=d.title||'';state.workDate=d.date||'';state.workText=d.text||'';state.audioBlob=d.audio||null;state.duration=Number(d.duration)||0;state.liveTranscript=d.transcript||'';state.voiceAttempted=Boolean(d.voiceAttempted);var files=Array.isArray(d.photos)?d.photos.filter(function(x){return x instanceof Blob}):[];revokeMediaUrls();state.workMedia=mediaFromFiles(files);state.newPhotoFiles=files.slice()}}
  catch(e){try{var p=JSON.parse(localStorage.getItem('talera-workblad-text-v2')||'null');if(p){state.workTitle=p.title||'';state.workDate=p.date||'';state.workText=p.text||''}}catch(_){}}
  draftLoaded=true;
}
async function draftSave(){if(!draftLoaded||state.editingStoryId)return;ensure();var photos=state.workMedia.filter(function(m){return m&&m.kind==='local'&&m.file}).map(function(m){return m.file});var d={id:'current',title:state.workTitle||'',date:state.workDate||'',text:state.workText||'',photos:photos,audio:state.audioBlob||null,duration:Number(state.duration)||0,transcript:state.liveTranscript||'',voiceAttempted:Boolean(state.voiceAttempted),savedAt:Date.now()};try{var db=await dbOpen();await new Promise(function(ok,no){var tx=db.transaction(STORE,'readwrite');tx.oncomplete=ok;tx.onerror=function(){no(tx.error)};tx.objectStore(STORE).put(d)});db.close()}catch(e){try{localStorage.setItem('talera-workblad-text-v2',JSON.stringify({title:d.title,date:d.date,text:d.text}))}catch(_){}}}
function draftSoon(){clearTimeout(draftTimer);draftTimer=setTimeout(draftSave,180)}
async function draftClear(){try{var db=await dbOpen();await new Promise(function(ok,no){var tx=db.transaction(STORE,'readwrite');tx.oncomplete=ok;tx.onerror=function(){no(tx.error)};tx.objectStore(STORE).delete('current')});db.close()}catch(e){}try{localStorage.removeItem('talera-workblad-text-v2')}catch(e){}}
function capture(){var a=document.getElementById('workTitle'),b=document.getElementById('workDate'),c=document.getElementById('workText');if(a)state.workTitle=a.value;if(b)state.workDate=b.value;if(c)state.workText=c.value}

function currentYear(){return new Date().getFullYear()}
function yearOptions(){var out='';for(var y=currentYear()+2;y>=1900;y--)out+='<option value="'+y+'">'+y+'</option>';return out}
function formatExact(value){var d=value?new Date(value+'T12:00:00'):null;if(!d||isNaN(d.getTime()))return '';return d.toLocaleDateString('nl-NL',{day:'numeric',month:'long',year:'numeric'})}
function setDateMode(next){dateMode=next==='approx'?'approx':'exact';if(!dateOverlay)return;var exact=dateOverlay.querySelector('#taleraDateExactPanel'),approx=dateOverlay.querySelector('#taleraDateApproxPanel');if(exact)exact.hidden=dateMode!=='exact';if(approx)approx.hidden=dateMode!=='approx';dateOverlay.querySelectorAll('.talera-date-tab').forEach(function(b){b.classList.toggle('active',b.dataset.mode===dateMode)})}
function closeDatePicker(){if(dateOverlay){dateOverlay.remove();dateOverlay=null}}
function openDatePicker(){
  if(dateOverlay)return;capture();dateOverlay=document.createElement('div');dateOverlay.className='talera-date-backdrop';dateOverlay.setAttribute('role','dialog');dateOverlay.setAttribute('aria-modal','true');
  dateOverlay.innerHTML='<div class="talera-date-sheet"><h2>Wanneer speelde dit verhaal zich af?</h2><p>Deze datum bepaalt waar je herinnering op de tijdlijn komt.</p><div class="talera-date-tabs"><button type="button" class="talera-date-tab active" data-mode="exact">Exacte datum</button><button type="button" class="talera-date-tab" data-mode="approx">Ongeveer</button></div><div id="taleraDateExactPanel" class="talera-date-panel"><label for="taleraExactDate">Kies een datum</label><input id="taleraExactDate" type="date"></div><div id="taleraDateApproxPanel" class="talera-date-panel" hidden><label>Welke periode?</label><div class="talera-date-approx"><select id="taleraApproxPeriod"><option value="jaar">Alleen het jaar</option><option value="lente">Lente</option><option value="zomer" selected>Zomer</option><option value="herfst">Herfst</option><option value="winter">Winter</option></select><select id="taleraApproxYear">'+yearOptions()+'</select></div></div><div class="talera-date-actions"><button type="button" class="talera-date-cancel">Annuleren</button><button type="button" class="talera-date-confirm">Gebruik deze datum</button></div></div>';
  document.body.appendChild(dateOverlay);dateMode='exact';var yr=dateOverlay.querySelector('#taleraApproxYear');if(yr)yr.value=String(currentYear());dateOverlay.querySelectorAll('.talera-date-tab').forEach(function(b){b.onclick=function(){setDateMode(b.dataset.mode)}});dateOverlay.querySelector('.talera-date-cancel').onclick=closeDatePicker;dateOverlay.addEventListener('click',function(e){if(e.target===dateOverlay)closeDatePicker()});
  dateOverlay.querySelector('.talera-date-confirm').onclick=function(){var label='';if(dateMode==='exact'){var v=dateOverlay.querySelector('#taleraExactDate').value;label=formatExact(v);if(!label){dateOverlay.querySelector('#taleraExactDate').focus();return}}else{var p=dateOverlay.querySelector('#taleraApproxPeriod').value||'jaar',y=dateOverlay.querySelector('#taleraApproxYear').value||'';if(!y)return;label=p==='jaar'?y:(p+' '+y)}state.workDate=label;var el=document.getElementById('workDate');if(el){el.value=label;el.classList.remove('work-required-missing')}state.workError='';draftSave();closeDatePicker()};
}

function photoHtml(){ensure();var input='<input id="workPhotoInput" class="work-photo-input" type="file" accept="image/*" multiple>';if(!state.workMedia.length)return '<div class="work-photo-section"><button id="workPhoto" class="work-photo" type="button"><span class="work-photo-empty"><span class="work-photo-plus">+</span><span>Voeg foto’s toe als je wilt</span></span></button>'+input+'</div>';var cover=state.workMedia[0],src=cover.localUrl||'',thumbs=state.workMedia.slice(0,6).map(function(m,i){return '<span class="work-photo-thumb '+(i===0?'active':'')+'"><img src="'+escAttr(m.localUrl||'')+'" alt=""></span>'}).join(''),extra=state.workMedia.length>6?'<span class="work-photo-more">+'+(state.workMedia.length-6)+'</span>':'';return '<div class="work-photo-section"><button id="workPhoto" class="work-photo has-photo" type="button"><img src="'+escAttr(src)+'" alt="Foto bij deze herinnering"><span class="work-photo-change">Foto’s toevoegen</span><span class="work-photo-count">'+state.workMedia.length+(state.workMedia.length===1?' foto':' foto’s')+'</span></button><div class="work-photo-strip">'+thumbs+extra+'</div>'+input+'</div>'}
function voiceHtml(){if(!state.audioBlob&&!state.hasExistingAudio)return '';return '<div class="work-voice-note"><span class="work-voice-mark"></span><span class="work-voice-copy"><strong>Gesproken verhaal toegevoegd</strong><span>'+fmt(state.duration)+' · klaar voor veilige opslag</span></span></div>'}
function renderWorkblad(){ensure();state.view='workblad';var editLabel=state.editingStoryId?'Herinnering bewerken':'Herinnering in aanbouw';app.innerHTML='<div class="work-stage"><div class="work-top"><div class="work-brand">TALERA</div></div><div class="work-scroll"><article class="work-sheet"><div class="work-kicker">'+editLabel+'</div><input id="workTitle" class="work-title" maxlength="140" placeholder="Waar gaat deze herinnering over?" value="'+escAttr(state.workTitle)+'" aria-required="true"><div class="work-date-row"><span class="work-date-dot"></span><input id="workDate" class="work-date" type="text" readonly placeholder="Wanneer speelde dit verhaal zich af?" value="'+escAttr(state.workDate)+'" aria-required="true" aria-haspopup="dialog"></div>'+photoHtml()+'<textarea id="workText" class="work-story" maxlength="20000" placeholder="Schrijf wat je wilt onthouden…">'+esc(state.workText)+'</textarea>'+voiceHtml()+(state.workError?'<div class="work-error">'+esc(state.workError)+'</div>':'')+'<div class="work-tools"><button id="workVoice" class="work-tool voice" type="button">'+(state.audioBlob?'Opnieuw inspreken':'Vertel iets')+'</button></div><div class="work-hint">Je hoeft dit niet in één keer af te maken.</div></article></div><div class="work-actions"><div class="work-draft">'+(state.editingStoryId?'Wijzigingen blijven bij dezelfde herinnering.':'Concept wordt op dit toestel bewaard.')+'</div><button id="workFinish" class="work-finish" type="button">Op mijn tijdlijn</button></div></div>';['workTitle','workText'].forEach(function(id){var el=document.getElementById(id);if(el)el.addEventListener('input',function(){capture();state.workError='';el.classList.remove('work-required-missing');draftSoon()})});var date=document.getElementById('workDate');if(date)date.onclick=function(e){e.preventDefault();openDatePicker()};var p=document.getElementById('workPhoto'),i=document.getElementById('workPhotoInput');if(p)p.onclick=photoPick;if(i)i.onchange=function(){var files=[].slice.call(i.files||[]);if(files.length)applyPhotoFiles(files)};document.getElementById('workVoice').onclick=voiceStart;document.getElementById('workFinish').onclick=finishWorkblad}
function photoPick(){capture();var i=document.getElementById('workPhotoInput');if(!i)return;i.value='';i.click()}
function applyPhotoFiles(files){ensure();var room=Math.max(0,12-state.workMedia.length),chosen=files.filter(function(f){return f&&String(f.type||'').indexOf('image/')===0}).slice(0,room);if(!chosen.length){if(room===0){state.workError='Voor deze herinnering houden we voorlopig maximaal 12 foto’s aan.';renderWorkblad()}return}chosen.forEach(function(f){state.workMedia.push({kind:'local',file:f,localUrl:URL.createObjectURL(f),name:f.name||'foto'});state.newPhotoFiles.push(f)});state.workError='';renderWorkblad();draftSave()}
function mergeSpoken(base,spoken){base=String(base||'').replace(/\s+$/,'');spoken=String(spoken||'').trim();if(!spoken)return base;return base+(base?'\n\n':'')+spoken}
function syncLiveTranscript(){state.workText=mergeSpoken(state.workTextBeforeVoice||'',state.liveTranscript||'');var el=document.getElementById('workText');if(el){el.value=state.workText;el.scrollTop=el.scrollHeight}}
function setupWorkRecognition(){var R=window.SpeechRecognition||window.webkitSpeechRecognition;if(!R)return;try{var r=new R();r.lang='nl-NL';r.continuous=true;r.interimResults=true;state.recognitionFinal='';r.onresult=function(e){var finalText=state.recognitionFinal||'',interim='';for(var i=e.resultIndex;i<e.results.length;i++){var piece=(e.results[i][0].transcript||'').trim();if(!piece)continue;if(e.results[i].isFinal)finalText+=(finalText?' ':'')+piece;else interim+=(interim?' ':'')+piece}state.recognitionFinal=finalText.trim();state.liveTranscript=(state.recognitionFinal+(interim?' '+interim:'')).trim();syncLiveTranscript()};r.onerror=function(){};r.onend=function(){if(state.stream&&state.view==='listening'){state.recognitionFinal=(state.liveTranscript||state.recognitionFinal||'').trim();try{r.start()}catch(e){}}};r.start();state.recognition=r}catch(e){}}
function appleRecorder(){var ua=String(navigator.userAgent||'');return /iPhone|iPad|iPod/i.test(ua)||(/Macintosh/i.test(ua)&&navigator.maxTouchPoints>1)}
function recorderMime(){var list=appleRecorder()?['audio/mp4','audio/webm;codecs=opus','audio/webm']:['audio/webm;codecs=opus','audio/webm','audio/mp4'];for(var i=0;i<list.length;i++){try{if(MediaRecorder.isTypeSupported&&MediaRecorder.isTypeSupported(list[i]))return list[i]}catch(e){}}return ''}
function audioFileName(blob){var type=String(blob&&blob.type||'').toLowerCase();return type.indexOf('mp4')>=0||type.indexOf('m4a')>=0?'verhaal.m4a':type.indexOf('ogg')>=0?'verhaal.ogg':type.indexOf('mpeg')>=0?'verhaal.mp3':'verhaal.webm'}
async function releaseRecorderSession(){if(state.silenceTimer){clearTimeout(state.silenceTimer);state.silenceTimer=null}var old=state.recorder;state.recorder=null;if(old){try{old.ondataavailable=null;old.onerror=null;old.onstop=null;if(old.state!=='inactive')old.stop()}catch(e){}}try{stopMedia()}catch(e){}var layer=document.getElementById('voiceLayer');if(layer)layer.remove();await sleep(120)}
async function voiceStart(){ensure();if(state.voiceStarting||state.view==='listening'||state.view==='mic-initializing')return;state.voiceStarting=true;capture();state.voiceAttempted=true;await draftSave();state.prevAudio=state.audioBlob||null;state.prevDuration=Number(state.duration)||0;state.prevTranscript=typeof state.liveTranscript==='string'?state.liveTranscript:'';state.prevWorkText=typeof state.workText==='string'?state.workText:'';await releaseRecorderSession();state.workTextBeforeVoice=state.workText||'';state.hasSpeech=false;state.lastSpeechAt=0;state.voice=0;state.paused=false;state.chunks=[];state.liveTranscript='';state.recognitionFinal='';state.startedAt=0;document.documentElement.style.setProperty('--voice','0');document.documentElement.style.setProperty('--awake','0');voiceLayer('Even luisteren…',true);try{state.stream=await navigator.mediaDevices.getUserMedia({audio:true});setupWorkAudio();setupWorkRecognition();startWorkRecorder();state.voiceStarting=false;voiceListening()}catch(e){state.voiceStarting=false;state.audioBlob=state.prevAudio;state.duration=state.prevDuration;state.liveTranscript=state.prevTranscript;state.workText=state.prevWorkText;try{stopMedia()}catch(_){ }state.workError='De microfoon kon niet worden geopend. Controleer de toestemming en probeer opnieuw.';renderWorkblad()}}
function voiceLayer(message,initializing){renderWorkblad();state.view=initializing?'mic-initializing':'listening';var stage=document.querySelector('.work-stage');if(stage)stage.classList.add('voice-open');var layer=document.createElement('div');layer.id='voiceLayer';layer.className='voice-layer';layer.innerHTML='<button id="voiceClose" class="voice-close" aria-label="Sluiten">×</button><div class="voice-center">'+core(false,!initializing)+'<div id="status" class="voice-status">'+esc(message)+'</div></div><div class="voice-bottom"><button id="singleControl" class="pill hidden">Klaar</button></div>';app.appendChild(layer);document.getElementById('voiceClose').onclick=voiceCancel;document.getElementById('singleControl').onclick=finishWorkSpeech}
function voiceListening(){var old=document.getElementById('voiceLayer');if(old)old.remove();voiceLayer('Neem je tijd. Ik luister.',false);state.view='listening'}
function setupWorkAudio(){state.context=new (window.AudioContext||window.webkitAudioContext)();state.analyser=state.context.createAnalyser();state.analyser.fftSize=256;state.source=state.context.createMediaStreamSource(state.stream);state.source.connect(state.analyser);var arr=new Uint8Array(state.analyser.frequencyBinCount);(function loop(){if(!state.analyser)return;state.analyser.getByteFrequencyData(arr);var sum=0;for(var i=0;i<arr.length;i++)sum+=arr[i];var n=Math.max(0,Math.min(1,(sum/arr.length-8)/42));state.voice=state.voice*.72+n*.28;document.documentElement.style.setProperty('--voice',state.voice.toFixed(3));if(state.voice>.08){state.lastSpeechAt=performance.now();if(!state.hasSpeech){state.hasSpeech=true;state.startedAt=Date.now();document.documentElement.style.setProperty('--awake','1');postEvent('first_speech',{mode:'workblad-v2'})}updateSpeechUi(true)}else updateSpeechUi(false);state.raf=requestAnimationFrame(loop)})()}
function finishWorkSpeech(){syncLiveTranscript();if(!state.hasSpeech){state.workError='Ik heb nog geen verhaal gehoord. Je kunt rustig beginnen wanneer je wilt.';voiceCancel();return}state.view='finishing';var r=state.recorder;if(r&&r.state!=='inactive'){try{if(typeof r.requestData==='function')r.requestData()}catch(e){}setTimeout(function(){try{if(r.state!=='inactive')r.stop()}catch(e){}},150)}else{try{stopMedia()}catch(e){}draftSave().finally(renderWorkblad)}}
function startWorkRecorder(){var mime=recorderMime();state.chunks=[];var r=new MediaRecorder(state.stream,mime?{mimeType:mime}:undefined);state.recorder=r;r.ondataavailable=function(e){if(e.data&&e.data.size)state.chunks.push(e.data)};r.onerror=function(){state.workError='De geluidsopname is onderbroken. Probeer het inspreken nog een keer.'};r.onstop=function(){var type=r.mimeType||mime||(appleRecorder()?'audio/mp4':'audio/webm'),b=new Blob(state.chunks,{type:type});state.recorder=null;syncLiveTranscript();if(state.hasSpeech&&b.size){state.audioBlob=b;state.duration=state.startedAt?Math.max(0,(Date.now()-state.startedAt)/1000):0;state.workError=''}else if(state.hasSpeech){state.audioBlob=state.prevAudio||null;state.duration=state.prevDuration||0;state.workError='Je woorden zijn wel herkend, maar de geluidsopname is niet vastgelegd. Probeer Vertel iets nogmaals.'}try{stopMedia()}catch(e){}state.view='workblad';draftSave().finally(renderWorkblad)};if(appleRecorder())r.start();else r.start(500)}
function voiceCancel(){state.voiceStarting=false;var r=state.recorder;state.recorder=null;if(r)try{r.ondataavailable=null;r.onerror=null;r.onstop=null;if(r.state!=='inactive')r.stop()}catch(e){}state.audioBlob=state.prevAudio===undefined?state.audioBlob:state.prevAudio;state.duration=state.prevDuration===undefined?state.duration:state.prevDuration;state.liveTranscript=state.prevTranscript===undefined?state.liveTranscript:state.prevTranscript;state.workText=state.prevWorkText===undefined?state.workText:state.prevWorkText;try{stopMedia()}catch(e){}state.hasSpeech=false;state.view='workblad';renderWorkblad()}

function metadataPayload(){var p=state.proposal||{};return {title:(state.workTitle||'').trim()||p.title||'',eventTime:(state.workDate||'').trim()||p.eventTime||'',place:p.place||'',people:p.people||'',eventTimePrecision:(state.workDate||'').trim()?'gebruiker':(p.eventTimePrecision||'onbekend'),adjusted:Boolean((state.workTitle||'').trim()||(state.workDate||'').trim())}}
async function uploadPhotoFiles(storyId,token,files){if(!files||!files.length)return;var fd=new FormData();files.slice(0,12).forEach(function(f){fd.append('media',f)});var res=await fetch('/api/stories/'+encodeURIComponent(storyId)+'/media',{method:'POST',headers:{'authorization':'Bearer '+token},body:fd}),data=await res.json();if(!res.ok)throw new Error(data.error||'Foto’s toevoegen mislukt')}
async function saveMetadata(storyId,token,meta){var res=await fetch('/api/stories/'+encodeURIComponent(storyId)+'/metadata',{method:'PUT',headers:{'content-type':'application/json','authorization':'Bearer '+token},body:JSON.stringify(meta)}),data=await res.json();if(!res.ok)throw new Error(data.error||'Gegevens opslaan mislukt')}
async function verifyStoredAudio(storyId,token){var path='/api/integration/stories/'+encodeURIComponent(storyId)+'/audio',head=await fetch(path,{method:'HEAD',headers:{'authorization':'Bearer '+token},cache:'no-store'}),bytes=Number(head.headers.get('content-length')||0);if(!head.ok||bytes<=0)throw new Error('De geluidsopname staat nog niet veilig op de server. Probeer het opnieuw.');var detailRes=await fetch('/api/integration/stories/'+encodeURIComponent(storyId),{headers:{'authorization':'Bearer '+token},cache:'no-store'}),detail={};try{detail=await detailRes.json()}catch(e){}if(!detailRes.ok||!detail.hasAudio||Number(detail.audioSizeBytes||0)<=0)throw new Error('De herinnering is nog niet aan de geluidsopname gekoppeld.');return detail}
async function saveEditedAudio(storyId,token){if(!(state.audioBlob instanceof Blob)||!state.audioBlob.size)return;var fd=new FormData();fd.append('audio',state.audioBlob,audioFileName(state.audioBlob));fd.append('durationSeconds',String(Number(state.duration)||0));var res=await fetch('/api/integration/stories/'+encodeURIComponent(storyId)+'/audio',{method:'PUT',headers:{'authorization':'Bearer '+token},body:fd,cache:'no-store'}),data={};try{data=await res.json()}catch(e){}if(!res.ok||!data.hasAudio)throw new Error(data.error||'De geluidsopname kon niet worden opgeslagen.');await verifyStoredAudio(storyId,token);state.hasExistingAudio=true}
async function finishWorkblad(){capture();state.workError='';await draftSave();var text=(state.workText||'').trim(),date=(state.workDate||'').trim(),title=(state.workTitle||'').trim();if(!title||!date){state.workError=!title&&!date?'Vul eerst een titel in en kies wanneer dit verhaal speelde.':(!title?'Vul eerst een titel in.':'Kies eerst wanneer dit verhaal speelde.');renderWorkblad();setTimeout(function(){var t=document.getElementById('workTitle'),d=document.getElementById('workDate');if(!title&&t){t.classList.add('work-required-missing');t.focus()}else if(!date&&d){d.classList.add('work-required-missing');openDatePicker()}},30);return}if(state.voiceAttempted&&(!(state.audioBlob instanceof Blob)||!state.audioBlob.size)&&!state.hasExistingAudio){state.workError='Je hebt Vertel gebruikt, maar er is geen geluidsbestand vastgelegd. Spreek het verhaal nogmaals in voordat je het opslaat.';return renderWorkblad()}if(!state.audioBlob&&!state.hasExistingAudio&&!text){state.workError='Voeg eerst iets toe: een gesproken stukje of wat tekst.';return renderWorkblad()}state.view='saving';var ov=document.createElement('div');ov.className='work-saving';ov.innerHTML='<div class="work-saving-card"><div class="processing"></div><h2>Je blad wordt op zijn plek gezet</h2><div class="muted">Je stem wordt eerst veilig opgeslagen. Daarna kom je terug bij deze herinnering op je tijdlijn.</div></div>';document.body.appendChild(ov);try{if(state.editingStoryId&&state.editingToken){await saveEditedAudio(state.editingStoryId,state.editingToken);var contentRes=await fetch('/api/integration/stories/'+encodeURIComponent(state.editingStoryId)+'/content',{method:'PUT',headers:{'content-type':'application/json','authorization':'Bearer '+state.editingToken},body:JSON.stringify({storyText:text})}),contentData=await contentRes.json();if(!contentRes.ok)throw new Error(contentData.error||'Opslaan mislukt');await saveMetadata(state.editingStoryId,state.editingToken,metadataPayload());await uploadPhotoFiles(state.editingStoryId,state.editingToken,state.newPhotoFiles||[]);state.storyId=state.editingStoryId;state.manageToken=state.editingToken}else{var fd=new FormData();if(state.audioBlob)fd.append('audio',state.audioBlob,audioFileName(state.audioBlob));if(text)fd.append('storyText',text);if(state.liveTranscript)fd.append('liveTranscript',state.liveTranscript);var localFiles=state.workMedia.filter(function(m){return m.kind==='local'&&m.file}).map(function(m){return m.file}),first=localFiles[0]||null;if(first)fd.append('startPhoto',first,first.name||'herinnering.jpg');fd.append('sourceMode','workblad');fd.append('durationSeconds',String(state.duration||0));fd.append('voiceAttempted',state.voiceAttempted?'1':'0');fd.append('eventTime',date);fd.append('title',title);fd.append('sessionId',sessionId);var name=localStorage.getItem('xxory-display-name');if(name)fd.append('displayName',name);var res=await fetch('/api/integration/stories',{method:'POST',body:fd,cache:'no-store'}),data={};try{data=await res.json()}catch(e){}if(!res.ok)throw new Error(data.error||'Opslaan mislukt');state.storyId=data.storyId;state.manageToken=data.manageToken;state.proposal=data.proposal||{};if(state.voiceAttempted){if(!data.hasAudio||Number(data.audioSizeBytes||0)<=0)throw new Error('De server heeft de geluidsopname niet bevestigd.');await verifyStoredAudio(state.storyId,state.manageToken)}if(localFiles.length>1)await uploadPhotoFiles(state.storyId,state.manageToken,localFiles.slice(1))}await draftClear();renderSavedWorkblad()}catch(e){ov.remove();state.workError=e.message||'Opslaan mislukt';renderWorkblad()}}
function handoffUrl(){return TIMELINE_URL+'#story='+encodeURIComponent(state.storyId||'')+'&token='+encodeURIComponent(state.manageToken||'')}
function goTimeline(){clearTimeout(redirectTimer);postEvent('return_to_timeline',{storyId:state.storyId,mode:'workblad-v2'});location.href=handoffUrl()}
function renderSavedWorkblad(){var ov=document.querySelector('.work-saving');if(ov)ov.remove();state.view='saved';app.innerHTML='<div class="work-stage"><div class="work-top"><div class="work-brand">TALERA</div></div><div class="work-scroll"><article class="work-sheet work-saved-sheet"><div class="work-saved-check">✓</div><h1>Je herinnering staat op zijn plek</h1><p>Je verhaal is opgeslagen. Je gaat nu terug naar precies deze herinnering op je tijdlijn.</p><div class="work-saved-actions one"><button id="timelineWork" class="work-tool voice" type="button">Naar mijn tijdlijn</button></div></article></div><div class="work-actions"><div class="work-draft">Opgeslagen</div><div></div></div></div>';document.getElementById('timelineWork').onclick=goTimeline;redirectTimer=setTimeout(goTimeline,700)}
function clearStateForNew(){closeDatePicker();revokeMediaUrls();state.workMedia=[];state.newPhotoFiles=[];state.audioBlob=null;state.hasExistingAudio=false;state.voiceAttempted=false;state.voiceStarting=false;state.duration=0;state.liveTranscript='';state.workTitle='';state.workDate='';state.workText='';state.workError='';state.storyId=null;state.manageToken=null;state.proposal=null;state.editingStoryId='';state.editingToken='';state.prevAudio=undefined;state.prevDuration=undefined;state.prevTranscript=undefined;state.prevWorkText=undefined}
function resetWorkblad(){clearTimeout(redirectTimer);clearStateForNew();draftClear().finally(renderWorkblad)}
async function prepareFreshNew(){var q=new URLSearchParams(location.search);if(q.get('new')!=='1')return false;await draftClear();clearStateForNew();draftLoaded=true;if(q.has('at')){q.delete('at');try{var qs=q.toString();history.replaceState(null,'',location.pathname+(qs?'?'+qs:'')+location.hash)}catch(e){}}return true}
function readEditContext(){var q=new URLSearchParams(location.search),id=q.get('edit')||'',h=new URLSearchParams(String(location.hash||'').replace(/^#/,'')),token=h.get('token')||'';if(id&&token)try{sessionStorage.setItem('talera-edit-token:'+id,token)}catch(e){}if(id&&!token)try{token=sessionStorage.getItem('talera-edit-token:'+id)||''}catch(e){}return id&&token?{id:id,token:token}:null}
async function loadExisting(ctx){ensure();state.editingStoryId=ctx.id;state.editingToken=ctx.token;state.storyId=ctx.id;state.manageToken=ctx.token;state.workMedia=[];state.newPhotoFiles=[];var res=await fetch('/api/integration/stories/'+encodeURIComponent(ctx.id),{headers:{'authorization':'Bearer '+ctx.token}}),data=await res.json();if(!res.ok)throw new Error(data.error||'Deze herinnering kon niet worden geopend.');state.workTitle=data.title||'';state.workDate=data.eventTime||'';state.workText=data.textContent||'';state.duration=Number(data.durationSeconds)||0;state.audioBlob=null;state.hasExistingAudio=Boolean(data.hasAudio);state.voiceAttempted=Boolean(data.hasAudio);state.liveTranscript='';var imageItems=(data.media||[]).filter(function(m){return m.mediaType==='image'}).slice(0,12),loaded=await Promise.all(imageItems.map(async function(item){try{var r=await fetch(item.url,{headers:{'authorization':'Bearer '+ctx.token}});if(!r.ok)return null;var blob=await r.blob();return {kind:'remote',id:item.id,localUrl:URL.createObjectURL(blob),mimeType:item.mimeType||blob.type,role:item.role||'extra'}}catch(e){return null}}));state.workMedia=loaded.filter(Boolean);draftLoaded=true}
async function boot(){ensure();var ctx=readEditContext();if(ctx){try{await loadExisting(ctx)}catch(e){state.workError=e.message||'Openen mislukt';draftLoaded=true}}else{var fresh=await prepareFreshNew();if(!fresh)await draftLoad()}renderWorkblad()}
renderEntry=renderWorkblad;activateMic=voiceStart;renderListening=voiceListening;setupAudioAnalysis=setupWorkAudio;startRecorder=startWorkRecorder;boot();
})();</scr`+`ipt>`;
